
  # ALLL IN Burgers

  This project contains the ALLL IN Burgers website exported from Figma Make.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
*** Add File: C:\Users\gurpr\alllinburgers-site\.gitignore
node_modules/
dist/
.DS_Store
npm-debug.log*
  
